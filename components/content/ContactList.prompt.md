Hairline-divided list of named contacts, each with a mono label above the mailto value.

```jsx
<ContactList items={[{ label: 'Nick Damen', value: 'nick.damen@example.com', href: 'mailto:nick.damen@example.com' }]} />
```
