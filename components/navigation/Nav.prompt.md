Sticky top nav: icon logo + wordmark, six text links (active one underlined blue), outline "Start a project" CTA on the right. Collapses to a hamburger below 820px in production (simplified here).

```jsx
<Nav activePath="/portal/" onNavigate={(path) => setRoute(path)} logoSrc="/assets/logo-icon-navy.png" />
```
