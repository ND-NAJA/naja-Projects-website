Section-opening hero over a faint blueprint grid: mono eyebrow with a short rule, big Space Grotesk headline (use an `<em>` for the orange highlight word), one-line dimmed description.

```jsx
<PageHero eyebrow="02 — Coaching" home={false}
  title={<>You don't need another course.<br/>You need a second <em style={{fontStyle:'normal', color:'var(--accent)'}}>opinion</em>.</>}
  description="For young and mid-career entrepreneurs building something real." />
```
